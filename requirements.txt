# Python 3.12. Install with: pip install -r requirements.txt
#
# torch is pinned to the version this project was developed against, but the
# correct wheel (CPU-only vs a specific CUDA build) depends on the target
# machine. If the default `pip install` doesn't give you the right build,
# install torch first following https://pytorch.org/get-started/locally/
# (select your OS/CUDA version) and then run this file with
# `pip install -r requirements.txt --no-deps torch` removed, e.g.:
#   pip install -r requirements.txt

numpy==2.4.4
scipy==1.17.1
pandas==3.0.2
torch==2.11.0
torchmetrics==1.9.0
optuna==4.8.0
matplotlib==3.10.8
tqdm==4.67.3
joblib==1.5.3
pytest==9.0.3
